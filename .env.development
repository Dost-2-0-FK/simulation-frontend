# Empty on purpose: requests go through the Vite dev proxy (see vite.config.ts)
# to /api on this same origin, which forwards to the local backend server-side
# and avoids the browser CORS restriction the backend doesn't handle itself.
VITE_API_BASE_URL=
